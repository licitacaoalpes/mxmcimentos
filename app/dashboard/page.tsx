import Dashboard from '@/components/Dashboard'

export const metadata = {
  title: 'Dashboard — MXM Cimentos',
}

// Server component — apenas renderiza o client component
export default function DashboardPage() {
  return <Dashboard />
}
